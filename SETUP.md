# دليل التشغيل خطوة بخطوة (Setup Guide)

هذا المشروع جاهز للعمل، تحتاج فقط لربطه بحسابات مجانية. اتبع الخطوات بالترتيب.

## 1) أنشئ حساب Supabase (قاعدة البيانات + تسجيل الدخول) — مجاني

1. اذهب إلى https://supabase.com وسجّل حساب مجاني (يمكن الدخول عبر GitHub أو Google).
2. اضغط "New Project". اختر اسم واختر باسورد لقاعدة البيانات (احتفظ به).
3. بعد إنشاء المشروع، اذهب إلى **SQL Editor** من القائمة الجانبية.
4. شغّل ملفات SQL التالية بالترتيب (انسخ محتوى كل ملف، الصقه في SQL Editor، اضغط **Run**، ثم انتقل للملف التالي):
   1. `supabase/schema.sql` — الجداول الأساسية والقواعد (منع تجاوز حد التذاكر، منع بيع أكثر من المتاح).
   2. `supabase/winner_selection.sql` — منطق اختيار الفائز تلقائياً (بيع كامل / انتهاء الوقت).
   3. `supabase/public_results_view.sql` — صفحة النتائج العامة مع إخفاء هوية الفائز جزئياً.
   4. `supabase/storage.sql` — صناديق تخزين صور المنتجات وإيصالات التحويل.
   5. `supabase/admin_stats_view.sql` — إحصائيات لوحة تحكم الإدارة.
5. اذهب إلى **Project Settings → API**. ستجد القيم التالية:
   - `Project URL`
   - `anon public` key
   - `service_role` key (سري — لا تشاركه مع أحد ولا تضعه في كود يُعرض في المتصفح)

## 2) أنشئ حساب GitHub (لتخزين الكود) — مجاني

1. اذهب إلى https://github.com وأنشئ حساب.
2. أنشئ Repository جديد فارغ (اسمه مثلاً `raffle-site`).
3. ارفع محتوى هذا المشروع إليه (يمكنك سحب وإفلات الملفات من واجهة GitHub، أو استخدام git لاحقاً).

## 3) انشر الموقع عبر Vercel — مجاني

1. اذهب إلى https://vercel.com وسجّل عبر حساب GitHub.
2. اضغط "Add New Project" واختر الـ Repository الذي رفعته.
3. عند إعداد المشروع، أضف متغيرات البيئة التالية (Environment Variables):
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `CRON_SECRET` — اختر أنت كلمة سر عشوائية طويلة (مثلاً 32 حرف عشوائي)، تُستخدم لحماية مهمة اختيار الفائز التلقائية.
4. اضغط **Deploy**. بعد دقيقة أو دقيقتين سيكون موقعك متاحاً على رابط مجاني مثل `raffle-site.vercel.app`.
   - ملف `vercel.json` في المشروع يجعل Vercel يشغّل مهمة فحص السحوبات (اختيار الفائز تلقائياً) كل 5 دقائق مجاناً — لا حاجة لإعداد إضافي.

## 4) لتشغيله على جهازك أولاً (اختياري، للتجربة قبل النشر)

```
npm install
cp .env.local.example .env.local
# عدّل .env.local وضع فيه القيم الحقيقية من Supabase، وأضف أيضاً CRON_SECRET
npm run dev
```

ثم افتح http://localhost:3000

## 5) لجعل أول مستخدم لك "أدمن"

1. سجّل حساب عادي في الموقع أولاً (صفحة /auth).
2. في Supabase، اذهب إلى **Table Editor → admins**، وأضف صفاً جديداً يدوياً:
   - `id`: انسخه من جدول `auth.users` (Authentication → Users) — هو نفس معرف حسابك.
   - `role`: `super_admin`
   - `status`: `active`
3. الآن يمكنك الدخول إلى:
   - `/admin` لوحة التحكم الرئيسية والإحصائيات
   - `/admin/draws/new` لإضافة سحب جديد
   - `/admin/deposits` للموافقة/رفض طلبات الشحن (مع عرض صورة الإيصال)
   - `/admin/winners` لتحديث حالة تسليم الجوائز

## ما الذي تم بناؤه حتى الآن

- قاعدة بيانات كاملة (مستخدمين، منتجات، سحوبات، تذاكر، مشتريات، طلبات شحن، معاملات مالية، فائزين، إشعارات، سجل تدقيق).
- حماية على مستوى قاعدة البيانات لمنع تجاوز حد 3 تذاكر أو بيع تذاكر أكثر من المتاح، حتى في حال الشراء المتزامن.
- صفحة السحوبات الرئيسية + صفحة تفاصيل السحب مع نافذة تأكيد الشراء.
- تسجيل الدخول / إنشاء حساب.
- الرصيد (شحن + سجل المعاملات).
- **اختيار الفائز تلقائياً** (سحب مكتمل البيع → يفوز بالمنتج، أو انتهاء الوقت → فائز عشوائي يحصل على 80% نقداً) عبر مهمة مجدولة كل 5 دقائق.
- صفحة "تذاكري" و"جوائزي" وصفحة "النتائج" العامة (مع إخفاء هوية الفائز جزئياً حفاظاً على الخصوصية).
- **إشعارات داخل الموقع** — جرس إشعارات في شريط التنقل مع تحديث تلقائي.
- **رفع صور المنتجات والإيصالات** مباشرة من الموقع عبر Supabase Storage (صور المنتجات عامة، الإيصالات خاصة لا يراها إلا صاحبها والإدارة).
- **لوحة إحصائيات الأدمن** (`/admin`) — عدد المستخدمين، إجمالي الشحن والمبيعات، السحوبات النشطة، الجوائز المستحقة، إلخ.
- **إدارة الفائزين والجوائز** (`/admin/winners`) — تحديث حالة التسليم (pending/verified/delivered/completed) مع إشعار تلقائي للفائز.
- لوحة أدمن: إضافة سحب جديد + الموافقة/رفض طلبات الشحن.

## ما لم يُبنَ بعد (المرحلة القادمة)

- التحقق من رقم الهاتف (OTP عبر SMS).
- تقسيم صلاحيات الأدمن الفعلي (حالياً أي صف في جدول admins يُعامل بنفس الصلاحيات؛ الجدول يدعم أدوار متعددة لكن الواجهة لا تفرّق بينها بعد).
- بحث/فلترة المستخدمين من لوحة الإدارة (تعليق حساب، إعادة تفعيل، مشاهدة سجل عملياته).
- صفحة "شروط السحب" الثابتة المعروضة قبل تأكيد الشراء (حالياً نص مختصر فقط داخل نافذة التأكيد).

قل لي أي جزء تريد أن أبنيه تالياً وسأكمل.
